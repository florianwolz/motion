{
  "name": "motion-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based editor and presentation shell for the Motion engine",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "tsc && node --test test/**/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.4.3",
    "vite-plugin-wasm": "^3.6.0"
  }
}
